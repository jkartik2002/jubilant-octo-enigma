{% render 'shogun-products', content: page %}
<section class="getting-started--hero">
  <div class="central content">
    <h1 class="title" style="background-size: cover;" data-cc-animate>{{ page.title }}</h1>

    <div class="rte" data-cc-animate data-cc-animate-delay="0.2s">
      {{ page.content }}
    </div>
  </div>
</section>

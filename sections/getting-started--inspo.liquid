<section class="staff-picks influencers inspo">
  <h2 class="title">{{ section.settings.title }}</h2>
  <div class="desc">{{ section.settings.desc }}</div>
  <a class="view-all-link" href="{{ section.settings.button-url }}">
    {{ section.settings.button-text }}
    <img alt="Right Arrow" class="arrow" src="{{ 'arrow-right.svg' | asset_url }}">
  </a>

  <div class="carousel carousel-insp d-none d-lg-block">
    {% for block in section.blocks %}
      <div class="carousel-cell" data-cc-animate="cc-fade-in-up" data-cc-animate-delay=".{{ forloop.index }}s">
        <a href="{{ section.settings.button-url }}">
          {% assign img_url = block.settings.image | img_url: '1x1' | replace: '_1x1.', '_{width}x.' %}
          <img 
            class="lazyload"
            {% if forloop == 1 %}
            src="{{ block.settings.image | img_url: '360x' }}"
            {% endif %}
            alt="{{ block.settings.title | escape | strip }}"
            data-src="{{ img_url }}"
            data-widths="[180,360,540,720,900,1080,1296,1512,1728,1944,2160,2376,2592,2808,3024]"
            data-aspectratio="{{ block.settings.image.aspect_ratio }}"
            data-sizes="auto"
            style="display: block; width: 100%; height: auto;"
            width="{{ block.settings.image.width }}"
            height="{{ block.settings.image.height }}"
            data-cc-animate="cc-fade-in-up"
          />
        </a>
      </div>
    {% endfor %}
  </div>

  <div class="carousel carousel-insp-xs d-block d-lg-none">
    {% for block in section.blocks %}
      <div class="carousel-cell" data-cc-animate="cc-fade-in-up" data-cc-animate-delay=".{{ forloop.index }}s">
        <a href="{{ block.settings.url }}">
          {% assign img_url = block.settings.image | img_url: '1x1' | replace: '_1x1.', '_{width}x.' %}
          <img 
            class="lazyload"
            {% if forloop == 1 %}
            src="{{ block.settings.image | img_url: '360x' }}"
            {% endif %}
            alt="{{ block.settings.title | escape | strip }}"
            data-src="{{ img_url }}"
            data-widths="[180,360,540,720,900,1080,1296,1512,1728,1944,2160,2376,2592,2808,3024]"
            data-aspectratio="{{ block.settings.image.aspect_ratio }}"
            data-sizes="auto"
            style="display: block; width: 100%; height: auto;"
            width="{{ block.settings.image.width }}"
            height="{{ block.settings.image.height }}"
            data-cc-animate="cc-fade-in-up"
          />
        </a>
      </div>
    {% endfor %}
  </div>

  <div class="carousel-pagination d-none {% if section.blocks.size > 4 %}d-lg-block{% endif %}">
    <span id="current-page-insp" class="num current-page">1</span>
    <span class="num divider">/</span>
    <span id="total-pages-insp" class="num total-pages">0</span>
  </div>

  <div class="progress-wrapper d-block d-lg-none">
    <div class="progress-bar"></div>
  </div>
  <script>
    var options = {
      cellAlign: 'left',
      contain: true,
      pageDots: false,
      imagesLoaded: true,
      wrapAround: true
    };

    {% if section.blocks.size > 4 %}
      var $inspirationCarousel = $('.carousel-insp').flickity(options);
    {% endif %}

    var $inspirationCarouselXS = $('.carousel-insp-xs').flickity(options);

    $(document).ready(() => {
      var size = $('div.carousel-insp div.carousel-cell').length,
          indexXS = $('div.carousel-insp-xs div.carousel-cell.is-selected').index() + 1,
          totalXS = $('div.carousel-insp-xs div.carousel-cell').length,
          percent = (indexXS / totalXS) * 100;

      $('#total-pages-insp').text(size);
      $('div.progress-bar').css('width',`${percent}%`);

      {% if section.blocks.size > 4 %}
        $inspirationCarousel.on('change.flickity', function(event, index) {
          $('#current-page-insp').text(index + 1);
        });
      {% endif %}


      $inspirationCarouselXS.on('change.flickity', function(event, index) {
        var indexXS = $('div.carousel-insp-xs div.carousel-cell.is-selected').index() + 1,
            totalXS = $('div.carousel-insp-xs div.carousel-cell').length,
            percent = (indexXS / totalXS) * 100;

        $('div.progress-bar').css('width',`${percent}%`);
      });
    })
  </script>
</section>

{% if section.blocks.size < 5 %}
  <style>
    section.staff-picks.influencers.inspo div.carousel-insp {
      white-space: nowrap;
    }

    section.staff-picks.influencers.inspo div.carousel-insp div.carousel-cell {
      display: inline-block;
      width: 25%;
    }
  </style>
{% endif %}


{% schema %}
  {
    "name": "Inspiration",
    "settings": [
      {
        "type": "text",
        "id": "title",
        "label": "Title",
        "default": "In Need of Some Inspo?"
      },
      {
        "type": "text",
        "id": "desc",
        "label": "Desc",
        "default": "Check out the Gallery to get a glimpse of how jetsetters are using Jet Set Candy to share their stories."
      },
      {
        "type": "text",
        "id": "button-text",
        "label": "Button Text",
        "default": "VIEW GALLERY"
      },
      {
        "type": "url",
        "id": "button-url",
        "label": "Button URL"
      }
    ],
    "blocks": [
      {
        "type": "carousel-cell",
        "name": "Carousel Cell",
        "settings": [
          {
            "id": "image",
            "type": "image_picker",
            "label": "Image"
          }
        ]
      }
    ],
    "presets": [
	    {
        "name": "Inspiration",
        "category": "Custom",
        "settings": {}
      }
    ]
  }
{% endschema %}